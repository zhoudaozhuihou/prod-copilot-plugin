# Output schema for /impact-analysis

Analyze blast radius for a change request or current git diff.

Output direct impact, transitive impact, affected execution flows, API/data/UI/test/release impact, Mermaid impact graph, verification plan, and safe implementation sequence.

Prefer GitNexus MCP/CLI output when supplied; otherwise use repo scan evidence.
