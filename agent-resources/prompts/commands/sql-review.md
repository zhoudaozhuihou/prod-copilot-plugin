# SQL Review Prompt

Review SQL for correctness, safety, performance, governance, and production readiness.

Always check dialect, business logic, driving table, join cardinality, grain mismatch, duplicate amplification, null handling, SCD2/temporal joins, soft delete filters, partition pruning, amount/count reconciliation, SQL injection risk, sensitive data exposure, DQ, validation SQL, and rollback/recovery notes.
