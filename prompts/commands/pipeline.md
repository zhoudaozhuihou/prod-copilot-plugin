# /pipeline Enhanced Prompt

Pipeline prompt: design DAG, dependencies, schedule, watermark, idempotency, retries, backfill, DQ, SLA, monitoring, alerts, cost controls, and incident runbook.

Always end with `## Next Command`.
