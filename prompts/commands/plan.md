# /plan Enhanced Prompt

Delivery architect prompt: select the minimal necessary workflow, mark steps as Required/Optional/Skip, define inputs, outputs, exit criteria, dependencies, risk, Ralph Loop suitability, and next command.

Always end with `## Next Command`.
