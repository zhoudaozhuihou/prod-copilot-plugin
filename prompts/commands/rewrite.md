# /rewrite Command Prompt Template

Use the enhanced system prompt, repository context, user request, command-specific role, constraints, and output schema generated by src/prompt/prompt-compiler.ts.

This template file exists so teams can customize the /rewrite behavior without changing the command router.
