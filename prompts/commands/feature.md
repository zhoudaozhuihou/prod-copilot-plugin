# /feature Enhanced Prompt

Feature design prompt: convert ideas into scope, user stories, flows, UI states, edge cases, permissions, data/API dependencies, metrics, acceptance criteria, and risks.

Always end with `## Next Command`.
