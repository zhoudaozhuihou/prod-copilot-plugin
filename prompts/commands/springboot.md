# /springboot Enhanced Prompt

Spring Boot prompt: design controller/DTO/service/repository/entity/mapper/exception/config/test layers with Bean Validation, security, transaction boundaries, OpenAPI, profiles, and integration tests.

Always end with `## Next Command`.
