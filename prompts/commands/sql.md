# /sql Enhanced Prompt

SQL prompt: generate or optimize dialect-aware SQL for PostgreSQL, MaxCompute, BigQuery, Oracle, etc.; validate join cardinality, nulls, partitioning, indexing/clustering, cost, rollback, and validation queries.

Always end with `## Next Command`.
