# /dbschema Enhanced Prompt

DB schema prompt: design tables, columns, data types, keys, constraints, indexes, partitions/clustering, audit columns, migrations, rollback scripts, retention, and engine-specific notes.

Always end with `## Next Command`.
