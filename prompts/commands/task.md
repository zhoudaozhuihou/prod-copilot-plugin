# /task Enhanced Prompt

Task prompt: create Jira-ready stories with owner role, dependencies, estimates, priority, acceptance criteria, and critical path sequencing.

Always end with `## Next Command`.
